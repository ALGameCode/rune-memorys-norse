using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Verificar Carregamento de todos os Managers, quando todos estiverem true, 
/// inicia contagem de x segundos e depois segue o fluxo de telas.
/// </summary>
public class LoadGame : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
